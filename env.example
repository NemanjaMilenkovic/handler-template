# Sentry Configuration
# Get these from https://sentry.io after creating a project
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
NEXT_PUBLIC_SENTRY_DSN=https://your-client-sentry-dsn@sentry.io/project-id

# Development Docker Sentry (local instance)
# Used when running the full Docker stack with local Sentry
SENTRY_SECRET_KEY=development-secret-key-change-in-production

# Grafana Configuration
GRAFANA_PASSWORD=admin123

# OpenTelemetry Configuration (Optional)
OTEL_SERVICE_NAME=handler-app
OTEL_SERVICE_VERSION=1.0.0
OTEL_RESOURCE_ATTRIBUTES=service.name=handler-app,service.version=1.0.0

# Application Configuration
NODE_ENV=production

# Database (if you add one later)
# DATABASE_URL=postgresql://user:password@localhost:5432/handler

# Redis (if you add caching later)
# REDIS_URL=redis://localhost:6379 