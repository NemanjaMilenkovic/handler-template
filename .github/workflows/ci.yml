name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    name: Build, Lint & Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # Read .nvmrc to get the Node.js version
      - name: Read .nvmrc
        run: echo "NODE_VERSION=$(cat .nvmrc)" >> $GITHUB_ENV

      # Setup Node.js using the version from .nvmrc and enable caching
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm' # Enable caching for pnpm

      # The pnpm/action-setup action will automatically read the pnpm version from package.json
      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint Code
        run: pnpm lint

      - name: Type Check
        run: pnpm tsc --noEmit

      - name: Build Projects
        run: pnpm build

      - name: Run Tests
        run: pnpm test

      - name: Security Audit
        run: pnpm audit --audit-level=high # It is better to use 'high'
